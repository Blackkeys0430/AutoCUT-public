# 电脑控制入口与最小故障修复

当前入口使用官方电脑控制组件，项目 `.codex/config.toml` 保持空配置。旧项目桥不是生产入口，不按本文件旧版安装 wrapper 或 sidecar。

## 按具体故障处理

- 日常不要求 CUA、SKY、Doctor 三套证明；只在实际调用失败时诊断。当前修复依据为 NORMAL Memo `memos/cua-repl-surface-bridge-20260905`。
- `Module not found`：定位当前真实安装的 `@oai/sky` 目录，再使用官方 `js_add_node_module_dir`、重置会话并重试导入。不要固定旧运行时哈希。
- `trusted Node process exited unexpectedly`：错误本身不足以证明桥接漂移。核对当前全局环境白名单；只有确认与已知缺项相同，才使用现有 `E:/ai项目/VPS/repair-desktop-runtime-env.py` 先只读预览、备份、再补缺失变量名称。不得填写变量值或伪造管道。
- 2026-09-06 该最小修复后，已实际完成原生窗口枚举、激活、截图和控件树读取，无需重启Codex。不能由此宣称全部输入动作或剪映草稿已通过验证。
- 未证实原因时不新建桥、覆盖配置或连续要求重启；修复以原先失败操作的实际复测结果为准。

## 目标与验收

启动剪映使用已核实的精确8.8可执行文件入口，并通过实际进程路径、版本及窗口结果确认目标；显示名称或应用登记项不能代替版本确认。不强制某一工具特有的 `process:` 字符串作为唯一启动方式。

先核对并激活窗口，再读取真实截图和控件树。用户自己验收无需Agent工具证明；原生控制恢复、草稿首页可见和用户画面验收分别记录。

剪映版本、启动和写入要求见[环境与写入规则](JIAN_YING_ENVIRONMENT_AND_PROFILES.md)。
