# AutoCUT 发布副本协作规则

本仓库只包含口播视频剪辑管线。先读 `README.md`、`jianying-adapter/docs/PIPELINE.md`，创作时读 `reference_videos/analysis/教程统一整理_v1/教程使用说明书_v1.md` 当前工作法。

使用现有 Whisper/VideoCut → EDL → FFmpeg → pyJianYingDraft/本地适配层 → 剪映 8.8 流程，不引入单视频专用Python脚本或第二套管线。保留已解决的共享能力；局部修复只处理受影响部分。

首版先完成内容组织、逐段声画设计和实际构图，再交统一 CandidateAssembler 装配。按 CandidatePlan 合同保留语义、资源、字幕、音频和必做画面要求；新建及整体重做使用 visual_planning_version=2，至少1处蒙版、3处原生画面特效，数量不代替创作判断。

先保证人物、产品和关键细节的有效尺寸，再安排普通字幕与明显更大的特殊信息字；同期同词只显示一份。保留选定完整预设的原动画与原配音效。

所有真实草稿只走 `jianying-adapter/scripts/write_candidate_plan.py` 唯一 Writer，明确测试/生产隔离和用户授权。不得清锁、覆盖唯一原稿或绕过写入检查。缺资源先解决依赖，不删必要需求来通过门禁。

不自动启动剪映、不擅自导出、上传或发布。程序检查不等于原生视觉或声音验收；用户打开并播放后判断。装配完成不额外开展固定截图、切片审片流程；用户明确要求的拍摄或画面审查按当次指令执行。

示例中的 `E:\AutoCUT`、`YOUR_USER` 和资源地址是匿名化示例，使用实际本机路径。素材、模型、字体和本地资源不随源码附带；运行前按环境文档配置。不要提交这些本地文件、凭据或客户数据。

保留作者、来源标识、许可和第三方归属，修改版本注明自己的改动。本项目只在用户明确要求时使用 HyperFrames；本地剪映任务不自动切换工具链。
